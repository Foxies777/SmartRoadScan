import { HomeButton } from "@features/HomeButton";
import { Card } from "@widgets/CardWidget";

const Points = () => {
  return (
    <div style={{
      height: '100vh',
      width: '100vw',
      overflowX: 'hidden',
      position: 'relative'
    }}>
      <div>привет</div>
      <Card/>
    </div>
  )
}

export default Points